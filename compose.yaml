services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: crm
      POSTGRES_PASSWORD: local-crm-only
      POSTGRES_DB: crm
    ports:
      - "127.0.0.1:55439:5432"
    volumes:
      - crm_data:/var/lib/postgresql/data
volumes:
  crm_data:
